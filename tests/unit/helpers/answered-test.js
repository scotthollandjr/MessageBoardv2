import { answered } from 'message-board/helpers/answered';
import { module, test } from 'qunit';

module('Unit | Helper | answered');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answered([42]);
  assert.ok(result);
});
