import { hideTrolls } from 'message-board/helpers/hide-trolls';
import { module, test } from 'qunit';

module('Unit | Helper | hide trolls');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hideTrolls([42]);
  assert.ok(result);
});
